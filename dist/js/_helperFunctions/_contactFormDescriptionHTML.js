const _contactFormDescriptionHTML = (name, email, subject, description) => {
  console.log(name, email, subject, description);
};

export { _contactFormDescriptionHTML }; // a list of exported variables
